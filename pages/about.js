import { Box, Text } from "@chakra-ui/react";
import Head from "next/head";
const Skills = ({ niceProps }) => {
  return (
    <Box {...niceProps}>
      <Text fontSize="4xl">The About Page</Text>
      <Text>This is page under active construction.</Text>
    </Box>
  );
};

export default Skills;
